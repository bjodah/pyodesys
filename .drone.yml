clone:
  depth: 2
  recursive: true
  submodule_override:
    external/anyode: git://github.com/bjodah/anyode.git
cache:
  mount:
    - /drone/sund-3.2.0-klu
    - /drone/sund-3.2.0-nolapack-noklu-extended
build:
  image: bjodah/bjodahimg18dev:v1.4
  environment:
    - OMP_NUM_THREADS=1
    - ANYODE_NUM_THREADS=2
    - CPLUS_INCLUDE_PATH=/usr/include/suitesparse
    - LIBRARY_PATH=/usr/lib/x86_64-linux-gnu
    - LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu
  commands:
    - git fetch -tq
    - if [ ! -d /drone/sund-3.2.0-klu ]; then .ci/get_sundials.sh 3.2.0 /drone/sund-3.2.0-klu -DLAPACK_ENABLE:BOOL=ON -DSUNDIALS_INDEX_SIZE=32 -DKLU_ENABLE:BOOL=ON -DKLU_INCLUDE_DIR=/usr/include/suitesparse -DKLU_LIBRARY_DIR=/usr/lib/x86_64-linux-gnu; fi
    - if [ ! -d /drone/sund-3.2.0-nolapack-noklu-extended ]; then .ci/get_sundials.sh 3.2.0 /drone/sund-3.2.0-nolapack-noklu-extended -DLAPACK_ENABLE:BOOL=OFF -DSUNDIALS_INDEX_SIZE=32 -DSUNDIALS_PRECISION:STRING="extended" -DKLU_ENABLE:BOOL=OFF; fi
    # Need to install pycvodes from source for extended precision/int64 test, since
    # that requires no LAPACK, whereas the pypi version installs with LAPACK
    - git clone --recurse-submodules https://github.com/bjodah/pycvodes.git /drone/pycvodes
    - cd /drone/pycvodes
    - LIBRARY_PATH=/drone/sund-3.2.0-nolapack-noklu-extended/lib:$LIBRARY_PATH CPLUS_INCLUDE_PATH=/drone/sund-3.2.0-nolapack-noklu-extended/include:$CPLUS_INCLUDE_PATH LD_LIBRARY_PATH=/drone/sund-3.2.0-nolapack-noklu-extended/lib:$LD_LIBRARY_PATH PYCVODES_LAPACK=0 PYCVODES_NO_KLU=1 python3 setup.py install
    - cd -
    - .ci/run_ci.sh pyodesys /drone/sund-3.2.0-nolapack-noklu-extended
    - rm -r /usr/local/lib/python*/dist-packages/pycvodes*
    # Need to remove cache so that new sundials/pyodesys doesn't try to use the old
    # compiled native code wrappers
    - rm -r /root/.cache/*pyodesys*
    # "Normal" (double, int) tests
    - .ci/run_ci.sh pyodesys /drone/sund-3.2.0-klu
    - ./scripts/prepare_deploy.sh
    - rm -r ~/.cache/python*pyodesys*/
    - git clean -fd
    - PATH=/opt/miniconda3/bin:$PATH conda config --add channels bjodah  # sym, pyodesys, pyneqsys
    - PATH=/opt/miniconda3/bin:$PATH conda upgrade --quiet conda-build
    - PATH=/opt/miniconda3/bin:$PATH conda build conda-recipe  # --output-folder "deploy/public_html/branches/${CI_BRANCH}"
    - bash -c '[[ $(python3 setup.py --version) =~ ^[0-9]+.* ]]'
    - ./scripts/grep-for-merge-blocking-token.sh
    - ./scripts/grep-for-binary-data.sh

deploy:

  rsync:
    host: hera.physchem.kth.se
    port: 22
    user: pyodesys
    source: deploy/
    target: ~
    recursive: true
    delete: false
